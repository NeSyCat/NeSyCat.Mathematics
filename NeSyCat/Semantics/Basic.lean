/-!
# NeSyCat Semantics

This module is the formalization home for the targets extracted from the
NeSy26 conference paper (snapshotted verbatim at
`target/nesy26-paper.tex`). Declarations here live in the
`NeSyCat.Semantics` namespace.

This file is currently a stub: it establishes the module and its
namespace so the `leanblueprint` scaffold (see `blueprint/`) has a Lean
target to link `\lean{...}` declarations against via
`scripts/blueprint.sh`'s declaration check. The itemized formalization
target — the actual definitions and theorems drawn from the paper — is
authored in a later ticket (T-P3).
-/

namespace NeSyCat.Semantics

end NeSyCat.Semantics
